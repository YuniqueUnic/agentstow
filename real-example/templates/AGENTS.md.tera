# AgentStow Demo Workspace

## Context
- Owner: {{ owner }}
- Workspace: {{ workspace_name }}
- Default profile: {{ default_profile }}
- Direct env sample: {{ env.DIRECT_ENV }}
- Override sample: {{ env.DUPLICATE_ENV }}

## Shared Fragment
{{ file.reference | trim }}

## Imported MCP Example
```json
{{ mcp_servers.filesystem | trim }}
```

```toml
{{ mcp_servers.filesystem | trim | toml }}
```

```yaml
{{ mcp_servers.filesystem | trim | yaml }}
```

## Codex MCP Example
```json
{{ mcp_servers.filesystem | trim | codex | json }}
```

```toml
{{ mcp_servers.filesystem | trim | codex | toml }}
```

## Working Rules
- Prefer `cargo fmt --all` before finishing Rust changes.
- Treat generated MCP config as reviewable artifacts.
- Keep secrets as `${VAR}` placeholders or `[REDACTED_SECRET]`.
