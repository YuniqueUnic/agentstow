# Codex Fixture Reference

- Root instructions come from `AGENTS.md`.
- Additional fallback guidance is provided by `TEAM_GUIDE.md`.
- Provider-native MCP config is rendered to `.codex/config.toml`.
- README is ordinary content and is not a default instruction file.
- The imported stdio server is always named `filesystem`.
- The explicit HTTP server is always named `tavily`.
