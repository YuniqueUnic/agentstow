[project]
name = "agentstow-e2e"
version = "0.1.0"
description = "AgentStow e2e and BDD tests"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9",
    "pytest-bdd>=8.1.0",
]

[tool.pytest.ini_options]
addopts = "-q --tb=short"
testpaths = ["tests"]
